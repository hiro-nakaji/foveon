.container-fluid.photo_vga_base
  .row-fluid
    = link_to(image_tag(@photo.photo_data.vga.to_s, class: 'img-polaroid photo_vga'), @photo.photo_data.url.to_s)
    .span4
      .row-fluid
        .span12= @photo.title
        - @photo.formatted_exif.each do |key, value|
          .row-fluid
            .span6
              = t('exif.attributes')[key]
            .span6
              = value
